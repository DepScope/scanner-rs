[package]
name = "test-project"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tokio = "1.32.0"
regex = "1.10"

[dev-dependencies]
criterion = "0.5"
proptest = "1.2"

[build-dependencies]
cc = "1.0"
